name: SonarCloud analysis
on: push
jobs:
  sonarcloud:
    runs-on: ubuntu-latest
    name: SonarCloud
    steps:
      - name: Analyze with SonarCloud
        uses: actions/checkout@v2
       # with:
          # Disabling shallow clone is recommended for improving relevancy of reporting
        #  fetch-depth: 0
      - name: SonarCloud Scan
        uses: sonarsource/sonarcloud-github-action@master
        with:
          projectBaseDir: Dubeyshagun29/HCL_Demo
          
          args: >
                -Dsonar.projectKey=dubeyshagun29
                -Dsonar.organization=Dubeyshagun29_HCL_Demo
              #-Dsonar.host.url=https://sonarcloud.io
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}  # Needed to get PR information
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}   # Generate a token on Sonarcloud.io, add it to the secrets of this repo with the name SONAR_TOKEN (Settings > Secrets > Actions > add new repository secret)
         # with:
          #  projectBaseDir: github/workspace/Dubeyshagun29/HCL_Demo/AltoroJ-AltoroJ-3.2
            #C:/Users/Shagun Dubey/OneDrive/Desktop/AltoroJ-AltoroJ-3.2/HCL_Demo/HCL_Demo/AltoroJ-AltoroJ-3.2 


 

